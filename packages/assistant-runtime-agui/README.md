# @wenchuantech/assistant-runtime-agui

Optional protocol adapter that converts AG-UI-style events into `@wenchuantech/assistant-protocol` events. It is intentionally separate from the main Runtime so applications using the Java/Spring AI company protocol do not inherit AG-UI dependencies or semantics.
